{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Fake Variables Schema",
  "description": "Schema for shared/fake-vars.json - defines non-existent CSS variables that LLMs commonly invent",
  "type": "object",
  "additionalProperties": {
    "type": "object",
    "required": ["description", "alternative", "reason"],
    "properties": {
      "description": {
        "type": "string",
        "description": "What the fake variable appears to do"
      },
      "alternative": {
        "type": "string",
        "description": "The correct token or value to use instead"
      },
      "reason": {
        "type": "string",
        "description": "Why this variable doesn't exist in Orion"
      }
    }
  },
  "examples": [
    {
      "--font-sans": {
        "description": "Non-existent: body text font",
        "alternative": "var(--font-secondary)",
        "reason": "Orion uses semantic font families, not generic names"
      }
    }
  ]
}
