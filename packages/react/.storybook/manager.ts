import { addons } from "@storybook/manager-api";

addons.setConfig({
  sidebar: {
    order: [
      "Getting Started",
      "Icons",
      "Components",
      "Sections",
      "Templates",
    ],
  },
});
