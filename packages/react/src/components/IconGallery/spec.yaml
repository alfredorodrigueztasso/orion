name: IconGallery
title: "IconGallery"
description: "Gallery of available icons"
category: "component"

props:
  className:
    type: "string"
    description: "Additional CSS class name"
    required: false

ai_rules:
  - "Never add brand prop (brand is global via ThemeProvider)"
  - "Use semantic color tokens only (var(--text-primary), var(--interactive-primary), etc)"
  - "Check data-theme context before styling (light/dark)"

examples:
  - title: "Basic Usage"
    code: |
      <IconGallery />
  - title: "With Props"
    code: |
      <IconGallery {...props} />

tokens:
  - "--spacing-4"
  - "--radius-control"
  - "--font-secondary"

related_components: ["Button", "Card", "Field", "Stack"]
